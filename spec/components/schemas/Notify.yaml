type: object
properties:
  id:
    type: integer
    format: uint64
  group_id:
    type: integer
    format: uint64
  created_at:
    $ref: '#/components/schemas/DateTime'
  header:
    type: string
  content:
    type: string
