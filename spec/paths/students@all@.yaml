get:
  tags:
    - students
  summary: addPet
  description: |
    Returns `count` students from offset `offset`.
  operationId: AddPet
  parameters: []
  security:
    - basicAuth: []
  responses:
    '200':
      description: 'OK, return data'
      headers: {}
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
                format: 'true'
              result:
                type: array
                items:
                  $ref: '#/components/schemas/Student'
            description: 'OK, return data'
  deprecated: false
